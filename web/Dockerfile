# Use Nginx (a lightweight web server)
FROM nginx:alpine

# Copy your HTML file into Nginx's web directory
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80 so it’s accessible on the web
EXPOSE 80